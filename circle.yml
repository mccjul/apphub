general:
 artifacts:
    - "~/build_output.zip" # Save APK's, Lint Results, and Test Results

machine:
  node:
    version: 0.10.22


dependencies:
  pre:
    - npm install
  override:
    #- cd android && ./gradlew build dependencies --info

test:
  override:
    - cd android && ./gradlew test --info
    - zip -r ~/build_output.zip app/build/outputs/
